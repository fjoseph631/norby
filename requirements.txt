# Pip dependencies for the Norby workspace.
# Install ROS packages first via rosdep, then:
#   pip install -r requirements.txt
#
# ROS deps (handled by package.xml / rosdep, not pip):
#   webots_ros2_driver, nav2_bringup, slam_toolbox, pointcloud_to_laserscan,
#   cv_bridge, radar_msgs, controller_manager, robot_state_publisher

# Core scientific / vision
numpy>=1.21
scipy>=1.7
opencv-python>=4.5
filterpy>=1.4.5

# Object detection / tracking (object_tracking package)
pyyolo>=0.2
tensorflow>=2.10
boxmot>=10.0

black